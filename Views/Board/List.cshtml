@model WebApplication6.Models.DocumentsData
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>List</title>
</head>
<body>
    <div>
        <table>
            <thead>
                <tr>
                    <th>번호</th>
                    <th>제목</th>
                    <th>글쓴이</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var document in Model.DummyDocuments)
                {
                    <tr>
                        <td>@document.Document_Number</td>
                        <td>@document.Title</td>
                        <td>@document.Writer</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>
</html>
